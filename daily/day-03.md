# Day 3 — SQL

* `NULL` & three-valued logic
* `NOT IN` vs `NOT EXISTS`
* `COUNT(*)` vs `COUNT(column)`
* `LEFT JOIN` + aggregation
* `ON` vs `WHERE`
* Date functions & monthly aggregation
* `ROW_NUMBER`, `RANK`, `DENSE_RANK`
* `LAG()` & multi-stage CTEs
* Grain before aggregation/window functions
