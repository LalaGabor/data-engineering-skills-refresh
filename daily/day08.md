1. Collections

Lists: indexing, iteration, adding/removing, common methods
Dictionaries: keys/values, get(), items(), update(), adding/changing values
Sets: uniqueness, membership, union, intersection, difference
Nested structures: especially list of dictionaries and dictionaries containing lists

2. Loops & control flow

for loops over collections
while loops with conditions
if / elif / else
Combining loops + conditions + collections for data processing
Nested loops: outer loop processes records, inner loop processes items inside a record

3. Comprehensions

List comprehensions
Filtering with if
Set comprehensions
Dictionary comprehensions
Combining multiple conditions with and
Knowing when a normal loop is clearer

4. Strings

Strings are immutable
strip() — remove surrounding whitespace
lower() / upper() — normalize text
replace() — substitute text
split() — turn a string into a list
Basic indexing into the resulting list
in, startswith(), endswith(), find()
Practical string cleaning inside dictionaries