Learning: Paths define where files exist. For now I have learned direct (relative) Path, but better solutions exist to be learned later

Learning context managers can be closed with return (most common pattern). Use at same indentation level as with:

Learning: logging.getLogger() creates a Logger object from the Logger class. This can be assigned a local variable name to distinguish which Logger object is emitting messages
in log book.