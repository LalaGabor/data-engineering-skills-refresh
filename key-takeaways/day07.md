- SQL Exercise: it is possible to run a correlated subquery while doing aggregations using a GROUP BY /HAVING
Python diagnostics: I am rusty...less than SQL but also more than SQL. more muscle memory less knowledge
Access Dictionary items without a key is possbile via list() but this suggests a list object might be the correct type for the object
sets automatically deduplication when called.
Reminder: "=" assigns a value to the variable name. Two variables can reference the same value. Eg. Customer = [1,2] and Customer2 = Customer than Customer2 is 
actually referencing what Customer is referencing which is [1,2]. Altering Customer2 alters the underlying referenced value, therefore it is altered for Customer as well.
Use .copy() for a "shallow" copy (relevant for nested lists dicts)